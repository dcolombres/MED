{
  "name": "uma-salud-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npm run db:init && npm run dev",
    "dev": "concurrently \"npm run dev:api\" \"npm run dev:web\"",
    "dev:api": "cd api && npm run start:dev",
    "dev:web": "cd web && npm run dev",
    "db:init": "cd api && npx prisma db push && echo 'Base de Datos SQLite inicializada localmente.'"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
